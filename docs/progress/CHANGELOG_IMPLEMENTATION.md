# Implementation Changelog

> Human-readable, phase-by-phase summary of what actually changed in the codebase. Complements `git log` — this is the narrative version.

## Phase 0 — Repository baseline & safety

- No application behavior changed.
- Added `CLAUDE.md` (agent operating contract) and `docs/progress/*` skeletons.
- Created feature branch `feat/bh-clearinghouse-mvp`.
- Recorded baseline environment, dependency versions, and command output in `docs/01-repository-baseline.md`.

## Phase 1 — Branding, navigation & route skeletons

- Rebranded the public site strings in `apps/web/.env` from MakerKit defaults to "BH Clearinghouse Simulator" (name, title, description — all public, non-secret config).
- Added nine placeholder customer routes under `apps/web/app/home/(customer)/`: `dashboard`, `claims`, `claim-batches`, `remittances`, `payers`, `documents`, `support`, `users`, `audit`. Each renders a shared `PlaceholderNotice` component (`apps/web/components/placeholder-notice.tsx`) stating the screen is a Phase-1 skeleton with synthetic-only data.
- Added `apps/web/app/support/{layout,page}.tsx` and `apps/web/app/admin/{layout,page}.tsx` — new protected portal skeletons, each using a shared `PortalHeader` component (`apps/web/components/portal-header.tsx`) that reuses the existing profile-account dropdown.
- Extended `apps/web/config/paths.config.ts` with the nine new customer paths plus `supportPortal` (`/support`) and `adminPortal` (`/admin`).
- Extended `apps/web/config/navigation.config.tsx` with a new "Clearinghouse" nav group linking all nine customer routes, each with a `lucide-react` icon.
- Refactored `apps/web/middleware.ts`: extracted the existing `/home/*` auth+MFA gate into a reusable `requireAuthHandler` function and applied it to `/support/*` and `/admin/*` as well. This is intentionally the same coarse "logged in" gate as `/home` — Phase 2 replaces it with organization-membership and role-based checks once those tables exist.
- Added new i18n keys to `apps/web/public/locales/en/common.json` for the new nav labels and portal titles.
- Added `apps/e2e/tests/navigation/protected-routes.spec.ts` — 22 Playwright cases covering unauthenticated redirect-to-sign-in and authenticated reachability for all 11 new routes.
- No database migrations. No new packages under `packages/features/`.

## Phase 2 — Organizations, memberships, roles & RLS

- Added migration `apps/web/supabase/migrations/20260718232603_organizations.sql`: tables `organizations`, `organization_memberships`, `roles`, `permissions`, `role_permissions`, `invitations`; view `user_profiles` (`security_invoker = true` wrapper over `accounts`); functions `has_org_access`, `has_permission`, `create_organization`, `accept_invitation`, `get_organization_members` (all `SECURITY DEFINER`, `set search_path = ''`, narrow `grant execute ... to authenticated`); RLS on every new table; installs the `pgtap` extension (first pgTAP suite in this repo); seeds the nine roles + full permission-matrix mapping sourced from the architecture doc.
- Added `apps/web/supabase/tests/database/organizations-rls.test.sql` — 18 pgTAP assertions: same-tenant read allowed, cross-tenant read denied (organizations table, `has_org_access`, `get_organization_members`), role-without-permission denied (with a positive-control counterpart), full invite→accept flow, wrong-email negative, expired-token negative, and `create_organization` bootstrap/isolation.
- Added package `packages/features/organizations` (`@kit/organizations`): schemas (`create-organization`, `invite-member`, `update-member-role`, `remove-member`, `revoke-invitation`, `accept-invitation`, `switch-organization`); server API (`organizations.api.ts`) and server actions (`server-actions.ts`, all via `enhanceAction`, running as the calling user so RLS is the real enforcement); components `OrganizationSwitcher(+Container)`, `CreateOrganizationDialog`, `InviteMemberDialog`, `OrganizationMembersPage`, `AcceptInvitationPanel`.
- Added package `packages/features/access-control` (`@kit/access-control`): `ROLES`/`PERMISSIONS` TS constants mirroring the SQL seed keys; server helpers `hasOrgAccess`/`hasPermission` (wrap the `has_org_access`/`has_permission` RPCs); client hook `useHasPermission`.
- Modified `apps/web/app/home/_components/home-sidebar.tsx` — added `OrganizationSwitcherContainer` to the sidebar header.
- Replaced the Phase 1 placeholder at `apps/web/app/home/(customer)/users/page.tsx` with a real members + pending-invitations view, gated on a server-computed `has_permission(org_id, 'members.invite')` check.
- Added `apps/web/app/home/(customer)/invitations/accept/page.tsx` (`?token=`) and `pathsConfig.app.acceptInvitation`.
- Added `apps/e2e/tests/organizations/organizations.spec.ts` — 3 Playwright cases: create-organization-and-see-it-in-switcher, owner-sees-members-and-can-invite, accept-invitation-page-error-handling. Not required by this phase's build-plan entry (which only specifies pgTAP) but added because building real UI without driving it in a browser isn't verification — see `docs/progress/DECISIONS.md` for the three real bugs this caught.
- `apps/web/next.config.mjs`: added `@kit/access-control` and `@kit/organizations` to `transpilePackages`/`optimizePackageImports`; added `devIndicators: false` (see Decisions/Known Issues).
- No changes to `apps/web/supabase/seed.sql` — the pgTAP suite creates and rolls back its own fixture data rather than relying on permanent seed rows.

## Phase 3 — Providers, facilities, patients, coverages

- Added migration `apps/web/supabase/migrations/20260719011359_entities.sql`: tables `providers`, `facilities`, `patients`, `subscribers`, `coverages`, `organization_payer_enrollments`, each org-scoped via `has_org_access(organization_id)` RLS (SELECT/INSERT/UPDATE), full tenancy contract (`organization_id`, `created_at/by`, `updated_at/by`, `deleted_at`), per-table `sim_*_id` cosmetic synthetic labels, and NPI `CHECK` constraints on `providers`/`facilities`; trigger functions `kit.check_subscriber_patient_org()` and `kit.check_coverage_org_consistency()` (both `SECURITY DEFINER`) enforcing cross-org FK consistency that RLS alone can't.
- Added `apps/web/supabase/tests/database/entities-rls.test.sql` — 22 pgTAP assertions covering org-owned CRUD, cross-tenant read/insert denial, an NPI-format negative, and both FK-consistency trigger negatives (cross-org and same-org patient/subscriber mismatch), across all six tables.
- Added package `packages/features/entities` (`@kit/entities`): `lib/npi.ts` (CMS NPI Luhn checksum validator, `80840`-prefixed); per-entity Zod schemas (Create/Update/Deactivate + a permissive `*FormSchema` for client-side dialog typing); `entities.api.ts` (read methods); six server-action files (`providers`/`facilities`/`patients`/`subscribers`/`coverages`/`enrollments.actions.ts`, all via `enhanceAction`, running as the calling user); components — `ProviderDialog`/`ProvidersTab`, `FacilityDialog`/`FacilitiesTab`, `EnrollmentDialog`/`EnrollmentsTab`, `PatientDialog`/`PatientsTab`, `SubscriberDialog`/`SubscribersTab`, `CoverageDialog`/`CoveragesTab`, and the tab-group wrappers `ProvidersPageContent`/`PatientsPageContent` — using `@kit/ui/data-table` (the plain TanStack Table wrapper) and `@kit/ui/tabs`.
- Added new routes `apps/web/app/home/(customer)/providers/page.tsx` and `.../patients/page.tsx` — both brand new (Phase 1 never created placeholders for these) — each server-loading the current org's data and rendering the corresponding `*PageContent`.
- Extended `apps/web/config/paths.config.ts` (`providers`, `patients`) and `apps/web/config/navigation.config.tsx` (new nav entries between Dashboard and Claims) and `public/locales/en/common.json`.
- Added `apps/e2e/tests/entities/entities.spec.ts` — 3 Playwright cases: add a provider + facility, add a patient, cross-org isolation (a second org sees neither). Not required by this phase's build-plan entry (pgTAP-only) but added for the same reason as Phase 2's UI suite — see `docs/progress/DECISIONS.md` for the real bug this caught.
- `apps/web/package.json` / `next.config.mjs`: added `@kit/entities` as a dependency and to `transpilePackages`/`optimizePackageImports`.
- No changes to `apps/web/supabase/seed.sql` — same rationale as Phase 2 (no stable demo org exists yet to attach permanent seed rows to); pgTAP fixtures + organic UI creation satisfy the build plan's "seed a few synthetic providers/patients" for now.

## Phase 4 — Payer directory & versioned rule admin

- Added migration `apps/web/supabase/migrations/20260719015820_payers.sql`: function `public.is_platform_admin()` (`SECURITY DEFINER`, checks for a `platform_super_admin` membership in any org); tables `payers`, `payer_aliases`, `payer_routes`, `payer_supported_transactions`, `payer_rules`, `payer_rule_versions`, `payer_test_profiles` — none carry `organization_id`; RLS reads open to any authenticated user, writes gated on `is_platform_admin()`. Seeds exactly ten `SIM-` payers (one per architecture category: `medicare_ffs`, `medicare_advantage`, `medicaid_ffs`, `medicaid_mco`, `commercial_ppo`, `commercial_hmo`, `blue_plan`, `tricare`, `marketplace`, `regional_bh`), plus an alias, a simulated clearinghouse route, and all 6 supported-transaction rows for each; two payers (`SIM-RBH-001`, `SIM-MDMCO-001`) seeded with `payer_test_profiles.default_outcome = 'denied'`; 27 versioned rules across `universal` (3), `claim_type` (2), `payer_edit` (10, one per payer), `adjudication` (10, one per payer), `informational` (2), each `payer_rule_versions` row carrying the `rejection_or_denial` designation.
- Added migration `apps/web/supabase/migrations/20260719020018_payers_fk_backfill.sql`: best-effort backfills `coverages.payer_id`/`organization_payer_enrollments.payer_id` from the Phase 3 `payer_label` text field by matching against the new directory, then adds the `coverages_payer_id_fkey`/`organization_payer_enrollments_payer_id_fkey` FK constraints — completes the deferral Phase 3 recorded in `docs/progress/DECISIONS.md`.
- Added `apps/web/supabase/tests/database/payers-rls.test.sql` — 14 pgTAP assertions: seed sanity (exactly 10 `SIM-` payers, none without the prefix), non-admin read-allowed, non-admin insert/update-denied for `payers`/`payer_rules`/`payer_rule_versions`, platform-admin insert/update-allowed, rule-versioning (both versions remain queryable after adding a second), and the rejection-vs-denial designation on a real seeded rule.
- Added package `packages/features/payers` (`@kit/payers`): `lib/csv.ts` (hand-written RFC4180-ish CSV parser/serializer); Zod schemas (`CreatePayerSchema`, `UpdatePayerSchema`, `SetPayerActiveSchema`, `SoftDeletePayerSchema`, `PayerFormSchema`, `PayerImportRowSchema`/`ParsedPayerImportRowSchema`/`ImportPayersSchema`); `payers.api.ts` (list/get); server actions (`createPayerAction`, `updatePayerAction`, `setPayerActiveAction`, `softDeletePayerAction`, `importPayersAction`, all via `enhanceAction`); components `PayerDialog`, `PayersAdminTable` (hand-rolled `useReactTable` with sorting, search, category filter, CSV export), `PayerCsvImportDialog`, `PayersDirectory` (read-only), `PayerSelect` (reusable picker, also used by `@kit/entities`).
- Added package `packages/features/payer-rules` (`@kit/payer-rules`, depends on `@kit/payers`): Zod schemas (`CreateRuleSchema`, `AddRuleVersionSchema`, `SetRuleActiveSchema`, `RuleFormSchema`, `RuleVersionFormSchema`); `payer-rules.api.ts` (list rules with joined payer + versions, list versions for a rule); server actions (`createRuleAction`, `addRuleVersionAction`, `setRuleActiveAction`); components `CreateRuleDialog`/`RuleVersionFields` (shared), `AddRuleVersionDialog`, `RuleVersionHistoryDialog`, `PayerRulesAdminTable`.
- Modified `packages/features/entities`: `coverage.schema.ts`/`payer-enrollment.schema.ts` changed `payerLabel: z.string()` to `payerId: z.string().uuid()`; `coverages.actions.ts`/`enrollments.actions.ts` added a `resolvePayerLabel` helper that looks up `payers.display_name` and sets both `payer_id` and the derived `payer_label`; `CoverageDialog`/`EnrollmentDialog` replaced the free-text payer input with `PayerSelect`; added `@kit/payers` dependency.
- Added `apps/web/app/api/v1/_lib/api-response.ts`: `newCorrelationId`, `apiError`/`apiOk` (structured envelope per `CLAUDE.md`), `requireApiUser`, `requirePlatformAdmin`, `parsePagination` — shared by every `/api/v1/*` route, first use of this surface in the repo.
- Added `apps/web/app/api/v1/payers/route.ts` (`GET` list with category/search/pagination, `POST` create), `apps/web/app/api/v1/payers/[id]/route.ts` (`PATCH` update), `apps/web/app/api/v1/payers/import/route.ts` (`POST` bulk upsert) — all admin-gated for writes via `requirePlatformAdmin`.
- Added routes `apps/web/app/admin/payers/page.tsx` and `apps/web/app/admin/payer-rules/page.tsx` (both check `is_platform_admin` server-side, render the admin table or `AdminAccessDenied`); replaced the Phase 1 placeholder at `apps/web/app/home/(customer)/payers/page.tsx` with `PayersDirectory`; modified `apps/web/app/admin/layout.tsx` to add a new `AdminNav` component; added `apps/web/components/admin-nav.tsx` and `apps/web/components/admin-access-denied.tsx`.
- Modified `apps/web/app/home/(customer)/providers/page.tsx` and `patients/page.tsx` to fetch and pass the active payer list into `PayerSelect`.
- `apps/web/package.json` / `next.config.mjs`: added `@kit/payers`/`@kit/payer-rules` to dependencies and `transpilePackages`/`optimizePackageImports`.
- Added `apps/e2e/tests/payers/payers.spec.ts` — 3 Playwright cases: directory search (all ten seeded payers listed, search narrows results), non-admin sees `Access restricted` on both admin pages, and a platform admin (granted via a documented `execSync`/psql test-setup helper — no UI bootstrap path exists) adds a payer, adds a rule, and imports a payer via CSV, all through the real UI.

## Phase 5 — Professional (837P) & institutional (837I) claim builders

- Added migration `apps/web/supabase/migrations/20260719030000_claims.sql`: tables `claims` (header, `status` scoped to `draft`/`validation_failed`/`validated`/`approved`, `last_validation_result` jsonb), `professional_claim_details`/`institutional_claim_details` (1:1 detail rows), `claim_diagnoses`, `claim_lines`, `claim_documents`, `claim_relationships`, `claim_batches` — all org-owned via `has_org_access`/`has_permission`; cross-org FK-consistency triggers `kit.check_claim_org_consistency()`, a shared `kit.check_claim_child_org_consistency()`, `kit.check_professional_details_provider_org()`, `kit.check_institutional_details_facility_org()`, `kit.check_claim_relationships_org()` (all reusing the Phase 3 pattern); two permissive `claims` UPDATE policies (`claims_update_edit`/`claims_update_approve`) enforcing `claims.create_edit` vs `claims.approve_submit` at the RLS layer; two `SECURITY INVOKER` RPCs `create_professional_claim`/`create_institutional_claim` for atomic header+detail inserts.
- Added `apps/web/supabase/tests/database/claims-rls.test.sql` — 21 pgTAP assertions: org-owned CRUD for both claim types (header + detail + diagnosis + line), cross-tenant read/insert denial, 8 cross-org FK-consistency trigger negatives (patient/coverage/billing-provider on `claims`, rendering-provider on `professional_claim_details`, facility on `institutional_claim_details`, `claim_diagnoses`/`claim_lines`/`claim_relationships` child-org checks), a remittance-specialist create-denied negative, and the RBAC pair proving a `claims_specialist` cannot approve (throws) while a `claims_manager` can.
- Added package `packages/features/claims` (`@kit/claims`): `lib/synthetic-codes.ts` (small labeled ICD-10/CPT-HCPCS/revenue/type-of-bill/POS reference lists), `lib/unsupported-loops.ts` (the deliberate 837P/837I out-of-scope loop list + rejection messages), `lib/validate-claim.ts` (the rule-engine predicate map + `evaluateClaimRules`); `schema/claim.schema.ts`, `schema/claim-diagnosis.schema.ts`, `schema/claim-line.schema.ts`; `server/claims.api.ts` (`ClaimsApi.listClaims`/`getClaim`), `server/claims.actions.ts` (`createProfessionalClaimAction`/`createInstitutionalClaimAction`/`updateClaimNotesAction`/`validateClaimAction`/`approveClaimAction`), `server/claim-diagnoses.actions.ts`, `server/claim-lines.actions.ts`, `server/validate-claim.server.ts` (the shared validation core used by both the action and the REST endpoint); components `ProfessionalClaimDialog`/`InstitutionalClaimDialog` (builders), `ClaimsList`, `ClaimDetailPageContent`, `ClaimDiagnosesSection`/`ClaimDiagnosisDialog`, `ClaimLinesSection`/`ClaimLineDialog`, `ValidationResultsPanel`.
- Added routes `apps/web/app/home/(customer)/claims/page.tsx` (replaced the Phase 1 placeholder — list + New Professional/Institutional builder buttons) and `apps/web/app/home/(customer)/claims/[id]/page.tsx` (new — detail page, diagnoses/lines, validate/approve, server-computed `canCreateEdit`/`canApprove` via `hasPermission`).
- Added `apps/web/app/api/v1/claims/route.ts` (`GET` list, `POST` create — dispatches to the right RPC by `claimType`, rejects unsupported-loop keys with `422` before schema validation), `apps/web/app/api/v1/claims/[id]/route.ts` (`PATCH` notes only), `apps/web/app/api/v1/claims/[id]/validate/route.ts`, `apps/web/app/api/v1/claims/[id]/approve/route.ts`. Extended `apps/web/app/api/v1/_lib/api-response.ts` with `requireOrgPermission` (org-scoped permission check, alongside the existing `requirePlatformAdmin`).
- `apps/web/package.json` / `next.config.mjs`: added `@kit/claims` as a dependency and to `transpilePackages`/`optimizePackageImports`.
- Added `apps/e2e/tests/claims/claims.spec.ts` — 4 Playwright cases: full build→add-diagnosis→add-line→validate→approve for both a professional and an institutional claim through the real UI; a validation-failure case asserting the exact DB-sourced explanation text appears when a required diagnosis is missing; an RBAC check (a `claims_specialist`, granted via a documented test-setup DB grant, sees no Approve action) combined with a REST negative proving `POST /api/v1/claims` rejects an unsupported-loop field with `422`; cross-org isolation.
- No changes to `apps/web/supabase/seed.sql` — same rationale as prior phases (no stable demo org exists yet); pgTAP fixtures + organic UI creation satisfy verification.

## Phase 6 — EDI processor & trace graph

- Added migration `apps/web/supabase/migrations/20260719040000_edi.sql`: widened `claims.status` to `varchar(30)` and extended its CHECK to add `submitted`/`accepted_for_adjudication` (dropping and recreating both `claims_update_edit`/`claims_update_approve` RLS policies first, since Postgres won't `ALTER COLUMN TYPE` on a column a policy references); tables `processing_jobs` (`claim_id UNIQUE` — the idempotency anchor), `edi_transactions` (`claim_id UNIQUE`, control numbers), `edi_payloads`, `acknowledgments`, `rule_evaluations`, `replay_attempts` (all org-owned, `has_org_access` read / `has_permission('claims.approve_submit')` write); `transaction_events` append-only (INSERT policy only, no UPDATE/DELETE grant to `authenticated`); cross-org triggers reusing Phase 5's `kit.check_claim_child_org_consistency()` plus a new `kit.check_edi_transaction_child_org_consistency()`.
- Added `apps/web/supabase/tests/database/edi-rls.test.sql` — 16 pgTAP assertions: org-owned CRUD/read for all 7 tables, the `processing_jobs`/`edi_transactions` `claim_id` uniqueness (the idempotency guarantee, proven at the DB level), cross-tenant read/insert denial, a cross-org FK-consistency trigger negative, the `claims.approve_submit` permission gate, and the append-only proof (`UPDATE`/`DELETE` on `transaction_events` both fail with `permission denied for table transaction_events` — no grant exists at all, so RLS is never even reached).
- Added app `apps/worker` (package name `worker`, in the existing `apps/*` workspace glob, no Next.js dependency): `src/lib/control-numbers.ts` (synthetic ISA13/GS06/ST02 generator), `src/lib/synthetic-x12.ts` (837P/837I + TA1/999/277CA payload generators, every payload's first line an explicit `SIMULATED -- NOT FOR PRODUCTION USE` marker), `src/lib/hash.ts` (sha256), `src/claim-processor.ts` (`ClaimProcessor` interface + `DeterministicClaimProcessor`: claims the `processing_jobs` idempotency slot → re-validates via `@kit/claims`' `loadClaimValidationContext`/`evaluateClaimRulesDetailed` → on any failure, one rejection `transaction_events` row and `claims.status → validation_failed`, no EDI generated → on success, generates control numbers + a synthetic 837 payload, writes `edi_transactions`/`edi_payloads`, walks TA1 → 999 → 277CA (each: inbound `edi_payloads` + `acknowledgments` + one `transaction_events` row, always accepted) → `claims.status → accepted_for_adjudication`).
- Refactored `packages/features/claims/src/lib/validate-claim.ts`: added `evaluateClaimRulesDetailed` (returns every applicable rule's pass/fail, not just failures) with `evaluateClaimRules` now a thin failures-only wrapper over it, for Phase 6's `rule_evaluations` audit trail. Refactored `packages/features/claims/src/server/validate-claim.server.ts`: extracted `loadClaimValidationContext` (claim fetch + rule resolution) as its own exported function so `apps/worker` can reuse the exact same rule-resolution logic `runClaimValidation` (Phase 5) uses, rather than duplicating it.
- Added package `packages/features/edi` (`@kit/edi`): `schema/submit-claim.schema.ts`, `server/submit-claim.actions.ts` (`submitClaimAction` — guards `claim.status === 'approved'`, then calls `DeterministicClaimProcessor` from `worker/claim-processor`), `server/edi.api.ts` (read side), `components/submit-claim-button.tsx` (renders only when `canSubmit && status === 'approved'`).
- Added package `packages/features/transaction-trace` (`@kit/transaction-trace`): `server/trace.api.ts` (`getClaimTrace` — ordered `transaction_events` with payer/rule joins), `components/transaction-trace-panel.tsx` (renders every architecture-doc trace field per event: name, category, timestamp, actor, claim/batch IDs, ISA13/GS06/ST02, payer, route, request/response hash, status, code, explanation, rule ID, correlation ID, previous event, next recommended action).
- Modified `apps/web/app/home/(customer)/claims/[id]/page.tsx`: added the Submit button and Transaction Trace panel below the existing claim detail content. Modified `packages/features/claims/src/components/{claim-detail-page-content,claims-list}.tsx`: added `submitted`/`accepted_for_adjudication` to the status-badge variant maps, and fixed `.replace('_', ' ')` → `.replaceAll('_', ' ')` (a real display bug caught once the column-width fix let the real status value render).
- Added `apps/web/app/api/v1/claims/[id]/submit/route.ts` (`POST` — honors a client-supplied `Idempotency-Key` header, falls back to `submit:{claimId}`; the real dedupe guarantee is `processing_jobs.claim_id` uniqueness regardless of header value) and `apps/web/app/api/v1/claims/[id]/trace/route.ts` (`GET`).
- `apps/web/package.json` / `next.config.mjs`: added `@kit/edi`, `@kit/transaction-trace`, `worker` as dependencies and to `transpilePackages`/`optimizePackageImports`.
- Added `apps/e2e/tests/edi/edi.spec.ts` — 2 Playwright cases: full build→validate→approve→submit for a professional claim, asserting control numbers and every trace event (claim_submitted, edi_generated, ta1_received, 999_received, 277ca_received, accepted_for_adjudication) appear with the expected fields, then a duplicate submit via the REST endpoint proving zero new trace/EDI rows are created; and a not-yet-approved claim rejected with `409` both from the UI (no Submit button rendered) and the REST endpoint.
- Added `docs/02-architecture.md` (new — the submit pipeline diagram, idempotency design, rejection-vs-denial enforcement). Updated `docs/05-claim-lifecycle.md` and `docs/03-data-model.md` with the Phase 6 EDI/trace section.
- No changes to `apps/web/supabase/seed.sql` — same rationale as prior phases.

## Phase 7 — Adjudication simulator, remittances & reconciliation

- Added migration `apps/web/supabase/migrations/20260719050000_remittances.sql`: `UPDATE payer_test_profiles SET denial_rule_code = ...` wiring the two Phase 4 `default_outcome = 'denied'` payers to their already-seeded adjudication-category `payer_rules`; extended `claims_status_check` with `paid`/`denied` (dropping/recreating the two `claims_update_*` RLS policies first, same pattern as Phase 6's column-width fix); tables `remittances` (`claim_id UNIQUE`, raw 835 payload + hash, `outcome`/`status`), `remit_claims` (`remittance_id UNIQUE`, per-claim payment breakdown), `remit_service_lines` (per-line breakdown), `claim_adjustments` (`SIM-` CARC/RARC, `rule_id → payer_rules`), `eft_traces` (`remittance_id UNIQUE`), `payment_matches` (`eft_trace_id UNIQUE`, insert gated on `remittances.post_payment` not `claims.approve_submit`) — all org-owned; new trigger functions `kit.check_remittance_child_org_consistency()` and `kit.check_remit_claim_child_org_consistency()`, with `remit_claims` getting two triggers (one per parent FK) after a pgTAP negative caught the first draft only checking one.
- Added `apps/web/supabase/tests/database/remittances-rls.test.sql` — 18 pgTAP assertions: org-owned CRUD for all six new tables (including `remit_service_lines`), the three uniqueness/idempotency guarantees (`remittances.claim_id`, `eft_traces.remittance_id`, `payment_matches.eft_trace_id`), cross-tenant read/insert denial, four cross-org FK-consistency trigger negatives (`remit_claims`→claim, `remit_claims`→remittance, `eft_traces`→remittance, `claim_adjustments`→remit_claim, `remit_service_lines`→remit_claim), and the RBAC pair proving `payment_matches` needs `remittances.post_payment` specifically (a `claims_manager`, who has `claims.approve_submit`, is still denied).
- Extended `apps/worker/src/lib/synthetic-x12.ts` with `generate835Payload` (ISA/GS/ST/BPR/TRN/N1/CLP/NM1/CAS.../SE/GE/IEA segments, `CLP02` = `'1'` paid / `'4'` denied, same `SIMULATED -- NOT FOR PRODUCTION USE` marker convention as the Phase 6 837/ack generators). Added `apps/worker/src/lib/simulated-adjustment-codes.ts` — `resolveDenialAdjustmentCode(fieldPath)` maps a denial rule's `field_path` to a fixed, clearly-`SIM-`-labeled CARC/RARC + explanation (never an asserted real X12 code meaning), with a generic fallback for any future denial rule not yet in the map; `CONTRACTUAL_ADJUSTMENT_CODE`/`CONTRACTUAL_ALLOWANCE_RATE` for the paid path's flat simulated 80/20 split.
- Extended `apps/worker/src/claim-processor.ts`: refactored the Phase 6 closure-based event-insert helper into a reusable `insertTransactionEvent`/`resolvePayerContext`; added `ClaimProcessor.adjudicate()` (fetches the claim + coverage's payer, reads `payer_test_profiles.default_outcome` — never a per-claim/per-payer-ID branch — computes payment or resolves the denial's triggering rule and CARC/RARC, generates the 835 payload + hash, inserts `remittances`/`remit_claims`/`remit_service_lines`/`claim_adjustments` (+ `eft_traces` if paid), emits `adjudicating` → `835_received` → `paid`/`denied` transaction events; idempotency via the `remittances.claim_id` unique-violation path, same shape as Phase 6's `processing_jobs` guard) and `ClaimProcessor.matchEft()` (inserts `payment_matches`, emits `eft_matched` → `posted`, idempotency via `payment_matches.eft_trace_id`).
- Added package `packages/features/remittances` (`@kit/remittances`): schemas (`AdjudicateClaimSchema`, `MatchEftSchema`); `adjudicateClaimAction`/`matchEftAction` (`enhanceAction`, guard claim/remittance status before calling the processor, revalidate the claim detail + remittances list paths); `RemittancesApi` (`listRemittances`, `getRemittance`, and `getClaimOutcomeStats` — deliberately two independent queries against `processing_jobs` and `remittances`, never a single merged query, so rejection rate and denial rate can never be computed from the same source by construction); components `AdjudicateClaimButton`, `MatchEftButton`, `RemittancesList`, `RemittanceDetailPanel`, `RejectionDenialRatesCard`.
- Modified `apps/web/app/home/(customer)/claims/[id]/page.tsx`: added the Adjudicate button and, once a claim reaches `paid`/`denied`, a `RemittanceDetailPanel` below the existing Submit button + Transaction Trace panel. Replaced the Phase 1 placeholder at `apps/web/app/home/(customer)/remittances/page.tsx` with `RemittancesList`. Replaced the Phase 1 placeholder at `apps/web/app/home/(customer)/dashboard/page.tsx` with `RejectionDenialRatesCard`.
- Added `apps/web/app/api/v1/remittances/route.ts` (`GET`, paginated list) and `apps/web/app/api/v1/remittances/[id]/match-eft/route.ts` (`POST`, guards `outcome === 'paid'`).
- `apps/web/package.json` / `next.config.mjs`: added `@kit/remittances` as a dependency and to `transpilePackages`/`optimizePackageImports`.
- Added `apps/e2e/tests/remittances/remittances.spec.ts` — 2 Playwright cases: (1) a paid claim against `SIM Commercial PPO Network` showing the exact $80.00-of-$100.00 (80%) contractual math, the `SIM-CARC-CO1` adjustment row, a generated `SIM-EFT-` trace, and a working Match EFT action transitioning the remittance to "Matched"; then a denied claim against `SIM Regional Behavioral Health Network` on the same org, showing the rule-derived `SIM-CARC-PA1`/"prior authorization" explanation and confirming no `SIM-EFT-` trace or Match EFT button appears anywhere; (2) the required acceptance sweep — one professional claim built/validated/approved/submitted/adjudicated against each of the ten seeded payers in one organization, asserting the outcome for every payer individually and the aggregate **exactly 8 paid / 2 denied**, then confirming `/home/dashboard` reads exactly `20.0%` denial rate and `0.0%` rejection rate. Fixed two real Playwright-only bugs surfaced while writing this suite (dialog value persistence across re-opens breaking text-based selectors; a missing payer enrollment causing a false rejection) — see `docs/progress/DECISIONS.md`.
- Added `docs/07-test-scenarios.md` (new). Updated `docs/05-claim-lifecycle.md` (adjudication/remittance lifecycle section) and `docs/06-payer-rules.md` (`denial_rule_code` wiring).
- No changes to `apps/web/supabase/seed.sql` — same rationale as prior phases.

## Phase 8 — Support portal, documents & audit

- Added migration `apps/web/supabase/migrations/20260719060000_support_documents_audit.sql`: new access-control primitives `has_role(role_key)` and `has_platform_permission(permission_key)` (generalize Phase 4's `is_platform_admin()`); `kit.check_nullable_claim_org_consistency()` (a nullable-`claim_id`-aware variant of Phase 5's `kit.check_claim_child_org_consistency()`); tables `support_tickets` (`sim_ticket_id`, `assigned_to`, ticket-scoped RLS), `support_assignments`, `support_messages` (`is_internal_note`), `support_ticket_documents` (two parent FKs, two triggers), `documents` (private, generic, `storage_path`/`mime_type`/`file_size_bytes`, optional `claim_id`), `document_access_events`, `support_access_sessions` (`WITH CHECK` requires caller = ticket's `assigned_to`; `reason NOT NULL`; `expires_at > started_at` constraint; `granted_scope text[]` defaulting to `'{}'`), append-only `audit_events` (INSERT-only grant); `kit.check_support_ticket_child_org_consistency()` and `kit.check_document_child_org_consistency()` (generic, reused across several child tables); new function `has_active_support_session(target_org_id)`, consulted only by `remittances_read` (Phase 7, re-created here to add the `OR` clause) and the new `documents_read` policy; new private Storage bucket `org_documents` (`public = false`, `file_size_limit = 10485760`, `allowed_mime_types` allowlist) with `storage.objects` RLS keyed on an `{organization_id}/...` path prefix via `storage.foldername(name)`.
- Added `apps/web/supabase/tests/database/support-documents-audit-rls.test.sql` — 30 pgTAP assertions: org-owned CRUD, cross-tenant read/insert denial, four cross-org FK-consistency trigger negatives, ticket-scoping (agent vs. manager), the full support-access-session lifecycle (assigned-ticket requirement denied, active session grants `documents`+`remittances` read, ended session revokes it, a separately-recorded naturally-expired session also has no access, customer-visible session history), Storage bucket privacy (`public = false`, cross-tenant `storage.objects` read denied), and audit append-only (`UPDATE`/`DELETE` both `permission denied`).
- Added `packages/features/access-control/src/server/has-permission.ts`: `hasRole()`/`hasPlatformPermission()` wrappers around the two new RPCs, alongside the existing `hasOrgAccess()`/`hasPermission()`.
- Added package `packages/features/audit` (`@kit/audit`): `logAuditEvent()` (the one shared write path every security-sensitive action calls), `AuditApi.listEvents()` (best-effort actor-name resolution via a second query against `accounts`, since `audit_events.actor_id` has no FK PostgREST can embed and `accounts`' own RLS only lets a caller read their own row), `AuditLogPanel` component.
- Added package `packages/features/documents` (`@kit/documents`): `CreateDocumentSchema` (allowed MIME types, 10 MiB max, mirrors the bucket's own limits), `createDocumentRecordAction` (metadata insert after a successful client-side Storage upload), `getDocumentSignedUrlAction` (60s-TTL signed URL, logs `document_access_events` + `audit_events`), `DocumentsApi.listDocuments`, components `DocumentUploadDialog` (client-side `client.storage.from('org_documents').upload(...)`, matching `@kit/accounts`'s avatar-upload pattern) and `DocumentsList` (View/Download via a synchronously-opened popup redirected once the signed URL resolves — see `docs/progress/DECISIONS.md` for why).
- Added package `packages/features/support` (`@kit/support`): schemas (`CreateTicketSchema`, `CreateMessageSchema`, `AssignTicketSchema`/`UpdateTicketStatusSchema`, `StartAccessSessionSchema`/`EndAccessSessionSchema`); actions `createTicketAction`, `createMessageAction`, `assignTicketAction` (self-assign, also inserts a `support_assignments` history row), `updateTicketStatusAction`, `startAccessSessionAction`/`endAccessSessionAction` (both call `logAuditEvent`); `SupportApi` (`listTickets`, `getTicket`, `listMessages`, `listAccessSessions`, `getActiveAccessSession`); components `CreateTicketDialog`, `TicketsList` (shared between the customer and support-side queues via a `basePath` prop), `TicketMessages` (internal-note checkbox shown only when `canPostInternalNotes`), `AssignTicketButton`, `TicketStatusSelect`, `AccessSessionControl` (the session start/end control, gated on an `isAssignedToMe` prop), `SupportAccessBanner`, `SupportAccessHistory` (customer-visible).
- Modified `packages/features/claims/src/server/claims.actions.ts` (`approveClaimAction` now logs `claim.approved`), `packages/features/edi/src/server/submit-claim.actions.ts` (`submitClaimAction` now logs `claim.submitted`, skipped for a duplicate-ignored no-op), `packages/features/organizations/src/server/server-actions.ts` (`updateMemberRoleAction` now fetches the prior role before updating and logs `member.role_changed` with a from/to metadata diff).
- Replaced the Phase 1 placeholders at `apps/web/app/home/(customer)/support/page.tsx` (ticket list + create + access history), added `apps/web/app/home/(customer)/support/[id]/page.tsx` (new — ticket detail, non-internal messages only), replaced `apps/web/app/home/(customer)/documents/page.tsx` and `apps/web/app/home/(customer)/audit/page.tsx`. Added `apps/web/app/support/tickets/page.tsx` (role-gated queue, `showOrganization`) and `apps/web/app/support/tickets/[id]/page.tsx` (assign-to-me, `AccessSessionControl`, the banner, and — only while a session is active — a read-only list of that org's remittances/documents).
- Added `apps/web/app/api/v1/support/tickets/route.ts` (`POST`), `apps/web/app/api/v1/support/tickets/[id]/messages/route.ts` (`POST`), `apps/web/app/api/v1/support/access-sessions/route.ts` (`POST`, both logs `audit_events` itself since the REST path doesn't go through the Server Action), `apps/web/app/api/v1/support/access-sessions/[id]/end/route.ts` (`POST`). Extended `apps/web/app/api/v1/_lib/api-response.ts` with `requirePlatformPermission` (mirrors `requirePlatformAdmin`/`requireOrgPermission` but for `has_platform_permission()`).
- `apps/web/package.json` / `next.config.mjs`: added `@kit/audit`, `@kit/documents`, `@kit/support` as dependencies and to `transpilePackages`/`optimizePackageImports`.
- Added `apps/e2e/tests/support/support.spec.ts` — 2 Playwright cases: the full ticket → document upload → agent self-assign → banner-gated session → session-scoped visibility → session end → customer-visible history → audit trail flow in one real-browser test; a second test proving a document is unreachable via the Storage bucket's public-object endpoint (even with a valid anon `apikey`) but reachable via the real signed URL. Fixed three real bugs surfaced while writing this suite (a function prop crossing the Server/Client Component boundary; `window.open()` called after an `await` getting popup-blocked; a missing `isAssignedToMe` UX gate) — see `docs/progress/DECISIONS.md`.
- Updated `docs/04-rbac-and-rls.md` (the audited support-access-session mechanism in full, documents privacy, updated RLS policy summary table, new pgTAP section) and `docs/03-data-model.md` (new Support/documents/Governance table group, replacing the Phase 1 placeholder).
- No changes to `apps/web/supabase/seed.sql` — same rationale as prior phases.
