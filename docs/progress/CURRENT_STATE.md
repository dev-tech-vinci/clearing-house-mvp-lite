# Current State

> Snapshot of where the build stands right now. Updated at the end of every phase.

- **Last completed phase:** Phase 8 — Support portal, documents & audit (complete)
- **Branch:** `feat/bh-clearinghouse-mvp`
- **Baseline commit:** `37def9c20b01a3514cf69b5b3383bef3e5ffbcb9`
- **App state:** Multi-tenant with synthetic healthcare entities, a global payer/rules directory, a scoped claims data model, a working synthetic EDI/trace pipeline, a deterministic adjudication simulator with remittance/reconciliation, and now an audited support portal with private document storage and append-only audit logging. Phase 2 added organizations/memberships/roles/RLS. Phase 3 added providers/facilities/patients/subscribers/coverages/organization_payer_enrollments. Phase 4 added the global payer directory + versioned, explainable rules. Phase 5 added `claims`/`professional_claim_details`/`institutional_claim_details`/`claim_diagnoses`/`claim_lines`/`claim_documents`/`claim_relationships`/`claim_batches`. Phase 6 added `20260719040000_edi.sql` (`processing_jobs`, `edi_transactions`/`edi_payloads`, `acknowledgments`, `rule_evaluations`, `replay_attempts`, append-only `transaction_events`; new `apps/worker` app + `packages/features/edi`/`transaction-trace`). Phase 7 added `20260719050000_remittances.sql` (`remittances`, `remit_claims`/`remit_service_lines`, `claim_adjustments`, `eft_traces`, `payment_matches`; `ClaimProcessor.adjudicate()`/`matchEft()`; `packages/features/remittances`). Phase 8 adds `20260719060000_support_documents_audit.sql`: `support_tickets`/`support_messages`/`support_assignments`/`support_ticket_documents` (ticketing, ticket-scoped support visibility), `documents`/`document_access_events` (private-bucket-backed generic document registry, separate from Phase 5's metadata-only `claim_documents`), `support_access_sessions` (the audited, time-limited, no-silent-impersonation mechanism — assigned ticket + typed reason + hard expiry + read-only-by-construction, enforced entirely at the DB layer), and append-only `audit_events`. Two new access-control primitives, `has_role(role_key)` and `has_platform_permission(permission_key)`, generalize Phase 4's `is_platform_admin()` pattern; `has_active_support_session(target_org_id)` is the only RLS bypass `support_manager`/`support_agent` ever get into tenant-owned data (extends `remittances`'/`documents`' `SELECT` policies only — never a write). New private Storage bucket `org_documents` (size/type allowlisted), served only via short-TTL signed URLs. New packages `packages/features/audit`, `packages/features/documents`, `packages/features/support`. `logAuditEvent()` wired into `approveClaimAction`/`submitClaimAction`/`updateMemberRoleAction`/the new support-session and document-access actions. New routes: `/home/support` (+ `/home/support/[id]`), `/home/documents`, `/home/audit` (all replacing Phase 1 placeholders); `/support/tickets` (+ `/support/tickets/[id]`, the support-side queue + session-scoped customer-data view + SUPPORT ACCESS ACTIVE banner). REST: `POST /api/v1/support/tickets`, `.../tickets/{id}/messages`, `POST /api/v1/support/access-sessions`, `.../access-sessions/{id}/end`. `platform_super_admin` still has no cross-org bypass (only gates writes to global reference data via `is_platform_admin()`); `support_manager`/`support_agent` now have exactly one, narrowly-scoped, fully-audited bypass.
- **Next phase:** Phase 9 — Python test client & automated suite (not started; do not start until this phase's evidence is reviewed)
