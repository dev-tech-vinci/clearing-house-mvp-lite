# Current State

> Snapshot of where the build stands right now. Updated at the end of every phase.

- **Last completed phase:** Phase 5 — Professional (837P) & institutional (837I) claim builders (complete)
- **Branch:** `feat/bh-clearinghouse-mvp`
- **Baseline commit:** `37def9c20b01a3514cf69b5b3383bef3e5ffbcb9`
- **App state:** Multi-tenant with synthetic healthcare entities, a global payer/rules directory, and a scoped claims data model. Phase 2 added organizations/memberships/roles/RLS. Phase 3 added providers/facilities/patients/subscribers/coverages/organization_payer_enrollments. Phase 4 added the global payer directory + versioned, explainable rules (`payers`, `payer_rules`, `payer_rule_versions`, etc. — no `organization_id`, admin-gated writes via `is_platform_admin()`) and wired the real `payer_id` FK on coverages/enrollments. Phase 5 adds `20260719030000_claims.sql`: `claims`, `professional_claim_details`, `institutional_claim_details`, `claim_diagnoses`, `claim_lines`, `claim_documents`, `claim_relationships`, `claim_batches` — all org-owned, cross-org FK-consistency triggers reusing the Phase 3 pattern, RLS gated on `has_permission('claims.create_edit'/'claims.approve_submit'/'claims.correct_resubmit')` (the RBAC matrix seeded in Phase 2 is now actually enforced for the first time, via two permissive UPDATE policies on `claims` that correctly deny a `claims_specialist` approval attempt). New package `packages/features/claims`: synthetic ICD-10/CPT/HCPCS/revenue/type-of-bill code lists, an unsupported-loop rejection list matching the architecture's deliberate 837P/837I subset, a deterministic validation engine wired to live Phase 4 `payer_rule_versions` rows (not hardcoded messages), builder dialogs for both claim types, and a claim detail page with diagnoses/lines/validate/approve. New routes `/home/claims` (list + builders) and `/home/claims/[id]` (detail). REST: `POST/GET /api/v1/claims`, `PATCH /api/v1/claims/{id}`, `POST /api/v1/claims/{id}/validate|approve`. `claim_documents`/`claim_relationships`/`claim_batches` have schema+RLS+pgTAP but no UI yet (documented deferrals). Platform/support roles still have no cross-org RLS bypass into tenant-owned data. No EDI generation, adjudication, or remittance logic exists yet.
- **Next phase:** Phase 6 — EDI/trace (837 generation, transaction events, deterministic ClaimProcessor) (not started; do not start until this phase's evidence is reviewed)
