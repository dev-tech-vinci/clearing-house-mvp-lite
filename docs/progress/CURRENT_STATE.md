# Current State

> Snapshot of where the build stands right now. Updated at the end of every phase.

- **Last completed phase:** Phase 4 — Payer directory & versioned rule admin (complete)
- **Branch:** `feat/bh-clearinghouse-mvp`
- **Baseline commit:** `37def9c20b01a3514cf69b5b3383bef3e5ffbcb9`
- **App state:** Multi-tenant with synthetic healthcare entities and a global payer/rules directory. Phase 2 added organizations/memberships/roles/RLS (`20260718232603_organizations.sql`). Phase 3 added `20260719011359_entities.sql`: `providers`, `facilities`, `patients`, `subscribers`, `coverages`, `organization_payer_enrollments`, all org-scoped via `has_org_access` RLS, plus two cross-org FK-consistency triggers. Phase 4 adds `20260719015820_payers.sql` (tables `payers`, `payer_aliases`, `payer_routes`, `payer_supported_transactions`, `payer_rules`, `payer_rule_versions`, `payer_test_profiles` — global reference data, no `organization_id`, readable by any authenticated user, writable only by `platform_super_admin` via the new `is_platform_admin()` helper; seeds exactly ten `SIM-` payers and 27 versioned, explainable rules across 5 categories) and `20260719020018_payers_fk_backfill.sql` (wires the real `payer_id` FK on `coverages`/`organization_payer_enrollments` that Phase 3 deferred). New packages `packages/features/payers` (directory CRUD, CSV import/export, `PayerSelect` picker) and `packages/features/payer-rules` (versioned rule admin). New routes `/home/payers` (read-only directory), `/admin/payers`, `/admin/payer-rules` (platform-admin-gated CRUD), plus a shared `AdminNav`/`AdminAccessDenied`. First use of the `app/api/v1/*` REST surface (`GET/POST /api/v1/payers`, `PATCH /api/v1/payers/{id}`, `POST /api/v1/payers/import`), with the structured error envelope from `CLAUDE.md` (idempotency keys and `audit_events` rows are not yet wired — deferred to Phase 6/8). Platform/support roles still have no cross-org RLS bypass into tenant-owned data (unchanged from Phase 2). No claims or EDI domain logic exists yet. No UI bootstrap path exists yet to grant `platform_super_admin` — see `docs/progress/KNOWN_ISSUES.md`.
- **Next phase:** Phase 5 — Professional (837P) & institutional (837I) claim builders (not started; do not start until this phase's evidence is reviewed)
